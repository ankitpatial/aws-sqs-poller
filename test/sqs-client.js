var sinon   = require('sinon'),
    expect  = require('chai').expect,
    sqs     = require('../lib/sqs-client');

describe('SimpleQueue', function () {
    describe('Queue Attribute', function () {
        it('Return the queue attributes', function (done) {

        });
    });
});